type Job {
  id: ID!
  title: String!
  description: String
  createdAt: String
  updatedAt: String
}

type Query {
  jobs: [Job!]
}

type Mutation {
    createJob(data: CreateJobInput!): Job!
}

input CreateJobInput {
  title: String!
  description: String
}